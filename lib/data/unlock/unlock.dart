import 'package:flutter/material.dart';

class SwipeUpUnlock extends StatelessWidget {
  const SwipeUpUnlock({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
